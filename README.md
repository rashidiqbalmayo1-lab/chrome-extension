# Create a simple repository with just the privacy policy
git init
git add privacy-policy.html
git commit -m "Add privacy policy"
git push origin main
# Enable GitHub Pages in repo settings → Pages → Source: main branch
